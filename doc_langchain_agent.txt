------------------------------------------------------------------------------------------ comand
poetry run pytest tests/ -v


simple RAG test
poetry run python -m src.app.cli ingest --pdf-dir data/pdf --index-dir data/index --reset
poetry run python -m src.app.cli query --index-dir data/index "이 문서의 핵심 요약은?"



git add -A
git commit -m "..."
git push




------------------------------------------------------------------------------------------ code review

핵심 5개 파일만 보면 전체 구조가 80% 잡혀.

src/app/cli.py (명령어/옵션, ingest/query 인터페이스)

src/rag/config.py (환경변수/기본값, 토큰 절약 파라미터)

src/rag/loaders.py (LlamaParse → fallback 로더 로직)

src/rag/ingest.py (청크/임베딩/Chroma 저장)

src/rag/query.py + src/rag/utils.py (retrieval, 컨텍스트 제한, 프롬프트/LLM 호출)